# Перекрёстное ревью — `tools/recolor.py` (2026-09-05)

Пробный прогон навыка `/cross-review`. Два независимых ревьюера, друг друга не видели.
Оркестратор (Fable, модель чата) слил список, убрал дубли и перепроверил каждый пункт в репо.

| Ревьюер | Канал | Семейство | Статус |
|---|---|---|---|
| A | `Agent` tool, `model: opus` | Claude Opus | ок, 10 findings |
| B | `Agent` tool, `model: fable`, чистый контекст | Claude Fable | ок, 7 findings |
| B (план) | GPT через OpenRouter / триада на Маке / Gemini | другое семейство | **недоступны**: OpenRouter 402 (нет кредитов), триада «временно выключена», Gemini 429 (quota 0) |

Итог: ревью внутри одного семейства (слабее, чем задумано), но и так 4 из 11 пунктов нашёл только один из двух.

## Сводная таблица

| № | Где | Что не так | Нашёл | Класс | Статус |
|---|---|---|---|---|---|
| 1 | `recolor.py:3, 21` | Докстринг обещает «порт `drawPreview` из index.html» и «tunables must match». Функции `drawPreview` и констант LP/CONTRAST/SHADOW_MUL в JS нет ни в одном коммите; кузов в канвасе берётся готовым JPG (`drawCar`, `imgFor`). Эталона для сверки не существует | оба | major | confirmed (grep) |
| 2 | `recolor.py:31` | `np.minimum(245, mask)/255` — альфа никогда не 1.0, белая база просвечивает на 3.9 % по всему кузову. Измерено на `side` #1a3556: в точке `x==LP` выходит (33,58,90) вместо (26,53,86). Тёмные PTS-цвета систематически светлее заказанных; противоречит «exact target colour» | оба | major | confirmed (два независимых замера) |
| 3 | `recolor.py:31` vs `index.html:413`, `build_masks.py:38,51` | Три потребителя одних масок трактуют их по-разному: непрерывная альфа с кэпом / порог >120 / порог <0.4. Края кузова в офлайн-рендере и в живом канвасе ведут себя по-разному | только A | minor | confirmed (сверка строк) |
| 4 | `recolor.py:34` | Оба clamp в `spec = min(0.94, max(0.30, 0.92-0.58*tlum))` мертвы: диапазон выражения [0.34, 0.92]. Признак того, что коэффициенты перенесены не из этого кода | только A | minor | confirmed (аналитически) |
| 5 | `recolor.py:53` | Нет проверки `argv` — без аргументов `IndexError` вместо usage | оба | minor | confirmed (запуск) |
| 6 | `recolor.py:54` | Hex не валидируется: `abc` → `ValueError` с пустой строкой, `1a3556ff` молча теряет байт, `gggggg` — невнятная ошибка | оба | minor | confirmed (запуск) |
| 7 | `recolor.py:56-59` | Каталог создаётся и виды пишутся по одному без предварительной проверки 12 входных файлов → при сбое остаётся частичный набор, на который ссылается `colors.json` (390 путей) | оба | minor | confirmed (по коду; сбой не воспроизводился) |
| 8 | `recolor.py:28, 38, 44-46` | float64 на весь кадр, мёртвая аллокация `out = np.empty_like(base)` (~50 МБ), обе ветви градиента считаются для всех пикселей. Замер: 2.3 с / 430 МБ на вид; float32 без мёртвой строки — 1.25 с / 252 МБ | оба | minor (perf) | confirmed (замеры A и B сходятся) |
| 9 | `recolor.py:29-31` | Маска читается `convert("RGB")` + no-op `resize` (все маски `L` 1920×1080 = база). Латентно: RGBA-маска с прозрачным фоном при RGB-конверсии потеряет альфу и перекрасит весь кадр | оба | minor | confirmed (PIL: mode/size) |
| 10 | `recolor.py:49` | `astype(uint8)` усекает, не округляет → смещение −0.5 уровня по всем каналам (89.996 → 89) | только A | minor | confirmed (логика numpy) |
| 11 | `recolor.py:53-61` | `color-id` и `hex` не сверяются с `colors.json`: опечатка в id = «сиротский» каталог без ошибки; hex из argv может разойтись с `colors[].hex`, который использует фильтр «dark» (`index.html:442`) | только B | minor | confirmed (по коду) |

Не дефекты (проверено): путь вывода совпадает с `colors.json`; `quality=88` даёт ту же субдискретизацию 4:2:0, что и база; выбор красного канала маски согласуется с JS; повторный прогон скрипта воспроизводит закоммиченные рендеры побитово.

## Что дальше (решение за Сашей)
- №2 — единственный пункт, меняющий картинку: убрать кэп 245 → перегенерировать 81 цвет × 6 видов и сверить с фото. Это продуктовое решение, не «тихая починка».
- №1, 4, 5, 6, 7, 8, 9, 10, 11 — механика, чинится одним пакетом через `/orchestrate` (implementer) за один круг; регенерация рендеров не требуется (кроме №10 — ±1 уровень при следующей регенерации).
- №3 — зафиксировать одну семантику маски и записать её в README.
